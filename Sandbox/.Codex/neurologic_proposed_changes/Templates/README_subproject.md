# Título del proyecto

Describe aquí la finalidad de tu subproyecto y el problema que resuelve. Explica en pocas líneas el contexto de negocio o técnico que motiva su existencia.

## Características principales

* Lista de funcionalidades clave.
* Dependencias principales (por ejemplo, módulos de `Core` que utiliza).
* Cualquier limitación o alcance excluido.

## Uso

1. Describe cómo compilar el proyecto (por ejemplo, `dotnet build` o `pwsh <script>`).
2. Explica cómo ejecutar la aplicación o las pruebas.
3. Indica dónde encontrar la documentación generada (`docs/`) y las tablas (`csv/`).

## Estructura del proyecto

```
NombreDelProyecto/
  AGENTS.md          # Reglas específicas para este proyecto
  README.md          # Este archivo
  docs/
    spec.md          # Especificación técnica formal
    filemap_ascii.txt# Mapa ASCII de archivos y carpetas
    table_hierarchy.json # JSON con jerarquía de módulos
    plan.md          # Plan de trabajo sugerido para el agente
  csv/
    root.csv         # Tabla resumen de módulos/clases
    ...
  src/               # Código fuente (C#, PowerShell u otros)
  tests/             # Pruebas automatizadas
```

## Información adicional

Incluye aquí enlaces a AGENTS globales, a la política de calidad y cualquier dato útil para los colaboradores.
