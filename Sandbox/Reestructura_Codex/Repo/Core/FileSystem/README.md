# FileSystem

Librería reutilizable para operaciones básicas de sistema de archivos (NTFS) y utilidades de manejo de rutas. Multi-target `net8.0;net7.0;net6.0`.

## Estructura
- `src/`: código fuente principal.
- `tests/`: pruebas automatizadas (xUnit) que validan los servicios públicos.
