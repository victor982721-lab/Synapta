<UserControl x:Class="ProyectoBase.GUI.Views.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="{StaticResource BackgroundBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="10">

            <Border Style="{StaticResource Card}">
                <StackPanel>

                    <TextBlock Style="{StaticResource TitleText}"
                               Text="Configuración del Sistema"/>

                    <TextBlock Style="{StaticResource BodyText}"
                               Text="Aquí podrás ajustar parámetros generales, integrar nuevas opciones y extender el comportamiento del sistema."/>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource Card}">
                <StackPanel>

                    <TextBlock Style="{StaticResource SubtitleText}"
                               Text="Sección en desarrollo"/>

                    <TextBlock Style="{StaticResource BodyText}"
                               Text="Esta vista evolucionará en etapas futuras para centralizar ajustes avanzados."/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>

</UserControl>
